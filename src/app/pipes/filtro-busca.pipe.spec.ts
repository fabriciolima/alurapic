import { FiltroBuscaPipe } from './filtro-busca.pipe';

describe('FiltroBuscaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroBuscaPipe();
    expect(pipe).toBeTruthy();
  });
});
