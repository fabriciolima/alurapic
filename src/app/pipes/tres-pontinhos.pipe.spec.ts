import { TresPontinhosPipe } from './tres-pontinhos.pipe';

describe('TresPontinhosPipe', () => {
  it('create an instance', () => {
    const pipe = new TresPontinhosPipe();
    expect(pipe).toBeTruthy();
  });
});
